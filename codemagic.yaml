workflows:
  ios-workflow:
    name: Nur-Mecca iOS Automatic Build
    max_build_duration: 60
    integrations:
      app_store_connect: "Codemagic_Key"
    environment:
      groups:
        - apple_auth 
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "com.milltech.nurmecca.app"
        DEVELOPMENT_TEAM: "JBMD9M6ZPC"
        APP_STORE_CONNECT_KEY_IDENTIFIER: "U2SA5VYGH7"
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Capacitor Sync
        script: |
          npx cap sync ios
          xcodebuild -resolvePackageDependencies -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME"
      
      - name: Build and Sign iOS App
        script: |
          # 1. Setup API Key
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$PRIVATE_KEY" | tr -d '\r' > ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8

          # 2. Fetch Signing Files
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --issuer-id "$ISSUER_ID" \
            --key-id "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          
          # 3. Detect Profile
          PROV_PROFILE_PATH=$(find ~/Library/MobileDevice/Provisioning\ Profiles -name "*.mobileprovision" | head -n 1)
          PROV_PROFILE_UUID=$(grep -aA1 "UUID" "$PROV_PROFILE_PATH" | grep -oE "[-A-Z0-9]{36}" | head -n 1)
          PROV_PROFILE_NAME=$(/usr/libexec/PlistBuddy -c "Print :Name" /dev/stdin <<< $(security cms -D -i "$PROV_PROFILE_PATH"))
          
          echo "Using Profile: $PROV_PROFILE_NAME ($PROV_PROFILE_UUID)"

          # 4. Create ExportOptions.plist
          cat <<EOF > ExportOptions.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store</string>
              <key>teamID</key>
              <string>$DEVELOPMENT_TEAM</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>$BUNDLE_ID</key>
                  <string>$PROV_PROFILE_NAME</string>
              </dict>
              <key>signingStyle</key>
              <string>manual</string>
              <key>signingCertificate</key>
              <string>Apple Distribution</string>
          </dict>
          </plist>
          EOF

          # 5. EXPANDED: TARGET-ID SPECIFIC INJECTION & CACHE BUSTING
          python3 -c "
          import sys, re, os
          uuid, name, team = '$PROV_PROFILE_UUID', '$PROV_PROFILE_NAME', '$DEVELOPMENT_TEAM'
          
          for root, dirs, files in os.walk('.'):
              for file in files:
                  if file == 'project.pbxproj':
                      path = os.path.join(root, file)
                      with open(path, 'r') as f: content = f.read()

                      # Step A: Locate the main Target ID to prevent mis-injection
                      # We search for the product name 'App' and find its associated target ID
                      target_search = re.search(r'([A-Z0-9]{24}) /\* App \*/', content)
                      target_id = target_search.group(1) if target_search else None

                      # Step B: Force Manual signing in TargetAttributes for THAT specific ID
                      if target_id:
                          attr_block = f'{target_id} = {{\n\t\t\t\t\tDevelopmentTeam = {team};\n\t\t\t\t\tProvisioningStyle = Manual;\n\t\t\t\t\tProvisioningProfile = \"{uuid}\";\n\t\t\t\t\tProvisioningProfileSpecifier = \"{name}\";'
                          content = re.sub(f'{target_id} = {{[^}}]*}}', attr_block + '\n\t\t\t\t};', content)

                      # Step C: Global Settings Purge (Remove any old identity/profile keys)
                      for key in ['PROVISIONING_PROFILE', 'PROVISIONING_PROFILE_SPECIFIER', 'CODE_SIGN_STYLE', 'DEVELOPMENT_TEAM']:
                          content = re.sub(f'{key} = [^;]+;', '', content)
                      
                      new_settings = (f'\n\t\t\t\tPROVISIONING_PROFILE = \"{uuid}\";' +
                                     f'\n\t\t\t\tPROVISIONING_PROFILE_SPECIFIER = \"{name}\";' +
                                     f'\n\t\t\t\tDEVELOPMENT_TEAM = {team};' +
                                     '\n\t\t\t\tCODE_SIGN_STYLE = Manual;' +
                                     '\n\t\t\t\tCODE_SIGN_IDENTITY = \"Apple Distribution\";')
                      
                      content = re.sub(r'(buildSettings = \{)', r'\1' + new_settings, content)

                      with open(path, 'w') as f: f.write(content)
          "

          # 6. ARCHIVE - Added -derivedDataPath to force a fresh build description
          mkdir -p build/ios/xcarchive
          rm -rf ~/Library/Developer/Xcode/DerivedData/* # Clear build cache
          
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            -archivePath build/ios/xcarchive/App \
            -derivedDataPath ./DerivedData \
            -authenticationKeyID "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            -authenticationKeyIssuerID "$ISSUER_ID" \
            -authenticationKeyPath ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8 \
            PROVISIONING_PROFILE="$PROV_PROFILE_UUID" \
            PROVISIONING_PROFILE_SPECIFIER="$PROV_PROFILE_NAME" \
            DEVELOPMENT_TEAM="$DEVELOPMENT_TEAM" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            clean archive | tee xcodebuild_archive.log

          # 7. EXPORT
          if [ ! -d "build/ios/xcarchive/App.xcarchive" ]; then
              echo "ERROR: Archive failed. Checking logs..."
              exit 1
          fi

          xcodebuild \
            -exportArchive \
            -archivePath build/ios/xcarchive/App.xcarchive \
            -exportPath build/ios/ipa \
            -exportOptionsPlist ExportOptions.plist \
            -authenticationKeyID "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            -authenticationKeyIssuerID "$ISSUER_ID" \
            -authenticationKeyPath ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8

      - name: Sync Back to Repository
        script: |
          git config --global user.email "builder@codemagic.io"
          git config --global user.name "Codemagic Builder"
          find . -name "project.pbxproj" -exec git add {} +
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "fix: explicit target attributes and derived data purge"
            git push origin HEAD:main
          else
            echo "No changes to sync."
          fi

    artifacts:
      - build/ios/xcarchive/*.xcarchive
      - build/ios/ipa/*.ipa