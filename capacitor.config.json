import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.milltech.nurmecca.app',
  appName: 'Nur-Mecca', // Fixed missing closing quote here
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;